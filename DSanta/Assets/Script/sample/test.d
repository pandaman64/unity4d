import System;
import UnityEngine;

class DKids : MonoBehaviour{
  Vector3 pos;
  void Start(){
  }

  void Update(){
    print(new String("aaa"w.dup));
  }

  void OnCollisionEnter(Collision collision){
    if(collision.get_gameObject.CompareTag(new String("present"w.dup))){

    }
  }
}
